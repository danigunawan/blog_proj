<!DOCTYPE html>
<html>
<head>
  <title>BlogProj</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class= "container">
  <%= link_to "Home", root_path %>
  ||
  <%= link_to "About", about_path %>
  ||
  <%= link_to "Posts", posts_path %>
  ||
  <%= link_to "MAKE NU POST", new_path %>
  ||
  <%= link_to "Kiss Tha Sky", contact_us_path %>

  ||

  <% if user_signed_in? %>
    Hello,<span style="color:blue"> <%= current_user.first_name %></span>!
    Welcome to the<span style="color:green"> CHRONIC BLERG </span>
    <%= link_to "Your Liked Posts", user_likes_path %>
  ||
    <%= link_to "Logout", sessions_path, method: :delete,
        data: {confirm: "are you sure you want to log out homie?"}%>
  <% else %>
    <%= link_to "Login", new_session_path %> |
    <%= link_to "Sign Up", new_user_path %>
  <% end %>

  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, class: "alert alert-info" %>
  <% end %>
  <hr>

<%= yield %>
  </div>
</body>
</html>
